<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop-2.5.xsd">

    <!-- Application components -->
    <context:component-scan base-package="com.epam.brest.course2015.social.app"/>
    <context:component-scan base-package="com.epam.brest.course2015.social.consumer"/>
    <context:component-scan base-package="com.epam.brest.course2015.social.test"/>

    <!-- Spring message source for <@spring.message code=""> taglib -->
    <bean id="messageSource" class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basename" value="messages"/>
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <!-- Property locations -->
    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:social-app.properties</value>
                <value>classpath:cdn.properties</value>
                <value>classpath:security-roles.properties</value>
                <value>classpath:email.properties</value>
            </list>
        </property>
    </bean>

    <!-- Freemarker configuration -->
    <bean id="freemarkerConfig"
          class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
        <property name="freemarkerSettings">
            <props>
                <prop key="incompatible_improvements">2.3.24</prop>
                <prop key="template_exception_handler">rethrow</prop>
                <prop key="default_encoding">UTF-8</prop>
            </props>
        </property>
        <property name="templateLoaderPath" value="/views/clear_design/"/>
    </bean>

    <!-- Freemarker view resolver -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver">
        <property name="cache" value="true"/>
        <property name="prefix" value=""/>
        <property name="suffix" value=".ftl"/>
        <property name="contentType" value="text/html;charset=UTF-8"/>
    </bean>

    <!--File uploader MessageConverter -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="1073741824"/>
    </bean>

    <aop:aspectj-autoproxy/>

    <!-- Social Logger implementation -->
    <bean class="com.epam.brest.course2015.social.test.SocialLogger"/>

    <bean id="restTemplate" class="org.springframework.web.client.RestTemplate">
        <property name="interceptors">
            <list>
                <bean class="com.epam.brest.course2015.social.consumer.SocialHeaderInterceptor"/>
            </list>
        </property>
    </bean>

    <!-- Social Mail implementation -->
    <bean class="com.epam.brest.course2015.social.mail.SocialMailImpl">
        <property name="mailSender" ref="socialMailSender"/>
    </bean>
    <bean id="socialMailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="${email.host}"/>
        <property name="port" value="${email.port}"/>
        <property name="username" value="${email.username}"/>
        <property name="password" value="${email.password}"/>
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">${email.smtp.auth}</prop>
                <prop key="mail.smtp.starttls.enable">${email.smtp.starttls.enable}</prop>
            </props>
        </property>
    </bean>

    <!-- Freemarker email generator -->
    <bean id="freemarkerMailConfig" class="freemarker.template.Configuration">
        <property name="directoryForTemplateLoading" value="templates"/>
    </bean>
    <!-- Social Mail implementation -->

    <!-- Cloudinary support -->
    <bean class="com.cloudinary.Cloudinary">
        <constructor-arg>
            <map>
                <entry key="cloud_name" value="${cloud.name}"/>
                <entry key="api_key" value="${api.key}"/>
                <entry key="api_secret" value="${api.secret}"/>
            </map>
        </constructor-arg>
    </bean>
    <!-- Cloudinary support -->

</beans>
